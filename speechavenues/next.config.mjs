/** @type {import('next').NextConfig} */
const nextConfig = {
  // Domains for image hosting go here.
  images: {
    domains: ["picsum.photos", "res.cloudinary.com"],
  },
};

export default nextConfig;
